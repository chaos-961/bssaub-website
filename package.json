{
  "name": "bssaub-website",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/check-admin-payload.mjs && vite build",
    "preview": "vite preview",
    "admin:payload": "node --env-file-if-exists=.admin.env scripts/build-admin-payload.mjs",
    "admin:check": "node --env-file-if-exists=.admin.env scripts/check-admin-password.mjs",
    "images:optimize": "node scripts/optimize-images.mjs",
    "icons:favicon": "node scripts/generate-favicons.mjs --write"
  },
  "dependencies": {
    "@fontsource-variable/instrument-sans": "^5.2.0",
    "@fontsource-variable/roboto-condensed": "^5.3.0",
    "firebase": "^12.16.0",
    "gsap": "^3.12.5",
    "lenis": "^1.1.20",
    "matter-js": "^0.20.0"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "vite": "^6.1.0"
  }
}
